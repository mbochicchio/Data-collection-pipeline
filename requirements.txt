# -----------------------------------------------------------------------
# Core runtime
# -----------------------------------------------------------------------
# NOTE: apache-airflow is NOT listed here — it is already in the base image.
# Versions of packages that conflict with Airflow constraints are left
# unpinned so that pip can resolve them automatically.

# FAB auth manager — required by Airflow 3.x for user authentication
apache-airflow-providers-fab

# DuckDB — embedded OLAP database used as the single source of truth
duckdb==1.2.2

# GitHub REST API client — version managed by Airflow constraint file
PyGithub

# HTTP client used directly for streaming repo archives
requests

# Load environment variables from .env file
python-dotenv

# -----------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------

# Retry decorator used in GitHub / Designite wrappers
tenacity
